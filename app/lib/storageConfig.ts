export const STORAGE_MODE = process.env.STORAGE_MODE || "local";

/**
 * Local storage paths
 * (Cloud mode me ye replace ho jaayega)
 */
export const LOCAL_PATHS = {
  image: "storage/images",
  audio: "storage/audio",
  video: "storage/videos",
};
